# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: sologid-tech-hub
metadata:
    template: azd-init@1.11.0
services:
    tailcast-main:
        project: .
        host: containerapp
        language: js
        dist: build
